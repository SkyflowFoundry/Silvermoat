name: Deploy Test Stack (Parallel)

on:
  pull_request:
    branches: [main]
    paths:
      - 'cdk/**'
      - 'lambda/**'
      - 'ui-insurance/**'
      - 'ui-retail/**'
      - 'tests/e2e/**'
      - 'scripts/**'
      - '.github/workflows/**'
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number for stack naming (optional)'
        required: false
      force_deploy:
        description: 'Force stack deployment even if no changes detected'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  detect-changes:
    name: Detect Changed Files
    runs-on: ubuntu-latest
    outputs:
      infrastructure_changed: ${{ steps.filter.outputs.infrastructure }}
      ui_changed: ${{ steps.filter.outputs.ui }}
      tests_changed: ${{ steps.filter.outputs.tests }}
      base_stack_name: ${{ steps.stack-name.outputs.base_stack_name }}
    steps:
      - name: Post workflow start comment
        if: github.event.pull_request.number
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `🚀 **Test deployment started** (Parallel Multi-Vertical)\n\n[View workflow run →](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})`
            });

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Detect file changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            infrastructure:
              - 'cdk/**'
              - 'lambda/**'
              - 'scripts/deploy-stack.sh'
            ui:
              - 'ui-insurance/**'
              - 'ui-retail/**'
              - 'ui-landing/**'
              - 'scripts/generate-architecture-diagram.py'
              - 'scripts/deploy-ui.sh'
            tests:
              - 'tests/e2e/**'
              - '.github/workflows/**'

      - name: Determine base stack name
        id: stack-name
        run: |
          if [ -n "${{ github.event.pull_request.number }}" ]; then
            BASE_STACK_NAME="silvermoat-test-pr-${{ github.event.pull_request.number }}"
          elif [ -n "${{ inputs.pr_number }}" ]; then
            BASE_STACK_NAME="silvermoat-test-pr-${{ inputs.pr_number }}"
          else
            BASE_STACK_NAME="silvermoat-test-${{ github.run_id }}"
          fi
          echo "base_stack_name=${BASE_STACK_NAME}" >> $GITHUB_OUTPUT
          echo "Base stack name: ${BASE_STACK_NAME}"

  # ========================================
  # Insurance Vertical Pipeline
  # ========================================

  deploy-insurance-infra:
    name: Deploy Insurance Infra
    needs: [detect-changes]
    if: |
      needs.detect-changes.outputs.infrastructure_changed == 'true' ||
      inputs.force_deploy == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      stack_name: ${{ steps.deploy.outputs.stack_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements-test.txt

      - name: Install dependencies
        uses: ./.github/actions/install-deps
        with:
          install-ui: 'false'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Setup AWS CDK
        uses: ./.github/actions/setup-aws-cdk

      - name: Bootstrap CDK
        uses: ./.github/actions/bootstrap-cdk

      - name: Deploy insurance stack
        id: deploy
        env:
          STACK_NAME: ${{ needs.detect-changes.outputs.base_stack_name }}
          VERTICAL: insurance
        run: |
          echo "Deploying insurance stack: ${STACK_NAME}-insurance"
          chmod +x scripts/deploy-stack.sh
          ./scripts/deploy-stack.sh
          echo "stack_name=${STACK_NAME}-insurance" >> $GITHUB_OUTPUT

  deploy-insurance-ui:
    name: Deploy Insurance UI
    needs: [detect-changes, deploy-insurance-infra]
    if: |
      always() &&
      (needs.detect-changes.outputs.ui_changed == 'true' || needs.deploy-insurance-infra.result == 'success' || inputs.force_deploy == 'true') &&
      (needs.deploy-insurance-infra.result == 'success' || needs.deploy-insurance-infra.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      api_url: ${{ steps.outputs.outputs.api_url }}
      web_url: ${{ steps.outputs.outputs.web_url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment
        uses: ./.github/actions/setup-env

      - name: Install dependencies
        uses: ./.github/actions/install-deps

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Build and deploy insurance UI
        env:
          STACK_NAME: ${{ needs.detect-changes.outputs.base_stack_name }}
          VERTICAL: insurance
        run: |
          chmod +x scripts/deploy-ui.sh
          ./scripts/deploy-ui.sh

      - name: Get stack outputs
        id: outputs
        run: |
          STACK_NAME="${{ needs.detect-changes.outputs.base_stack_name }}-insurance"

          OUTPUTS=$(aws cloudformation describe-stacks \
            --stack-name "${STACK_NAME}" \
            --query 'Stacks[0].Outputs' \
            --output json)

          API_URL=$(echo "$OUTPUTS" | jq -r '.[] | select(.OutputKey=="InsuranceApiUrl") | .OutputValue')
          WEB_URL=$(echo "$OUTPUTS" | jq -r '.[] | select(.OutputKey=="InsuranceUiBucketWebsiteURL") | .OutputValue')

          echo "api_url=${API_URL}" >> $GITHUB_OUTPUT
          echo "web_url=${WEB_URL}" >> $GITHUB_OUTPUT

  insurance-tests:
    name: Insurance E2E Tests
    needs: [deploy-insurance-infra, deploy-insurance-ui]
    if: always() && needs.deploy-insurance-ui.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment for E2E tests
        uses: ./.github/actions/setup-env

      - name: Install Python dependencies for E2E tests
        run: pip install -r requirements-test.txt

      - name: Configure AWS credentials for E2E tests
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Install Chrome and ChromeDriver
        uses: browser-actions/setup-chrome@v1

      - name: Run insurance E2E tests
        env:
          INSURANCE_URL: ${{ needs.deploy-insurance-ui.outputs.web_url }}
          INSURANCE_API_URL: ${{ needs.deploy-insurance-ui.outputs.api_url }}
          HEADLESS: 'true'
        run: |
          cd tests/e2e
          pytest -v --tb=short -m "insurance"

  seed-insurance-data:
    name: Seed Insurance Data
    needs: [deploy-insurance-ui, insurance-tests]
    if: |
      always() &&
      needs.deploy-insurance-ui.result == 'success' &&
      (needs.insurance-tests.result == 'success' || needs.insurance-tests.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Seed insurance demo data
        uses: ./.github/actions/clear-and-seed-data
        with:
          api-base-url: ${{ needs.deploy-insurance-ui.outputs.api_url }}
          vertical: insurance

  # ========================================
  # Retail Vertical Pipeline
  # ========================================

  deploy-retail-infra:
    name: Deploy Retail Infra
    needs: [detect-changes]
    if: |
      needs.detect-changes.outputs.infrastructure_changed == 'true' ||
      inputs.force_deploy == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      stack_name: ${{ steps.deploy.outputs.stack_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements-test.txt

      - name: Install dependencies
        uses: ./.github/actions/install-deps
        with:
          install-ui: 'false'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Setup AWS CDK
        uses: ./.github/actions/setup-aws-cdk

      - name: Bootstrap CDK
        uses: ./.github/actions/bootstrap-cdk

      - name: Deploy retail stack
        id: deploy
        env:
          STACK_NAME: ${{ needs.detect-changes.outputs.base_stack_name }}
          VERTICAL: retail
        run: |
          echo "Deploying retail stack: ${STACK_NAME}-retail"
          chmod +x scripts/deploy-stack.sh
          ./scripts/deploy-stack.sh
          echo "stack_name=${STACK_NAME}-retail" >> $GITHUB_OUTPUT

  deploy-retail-ui:
    name: Deploy Retail UI
    needs: [detect-changes, deploy-retail-infra]
    if: |
      always() &&
      (needs.detect-changes.outputs.ui_changed == 'true' || needs.deploy-retail-infra.result == 'success' || inputs.force_deploy == 'true') &&
      (needs.deploy-retail-infra.result == 'success' || needs.deploy-retail-infra.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      api_url: ${{ steps.outputs.outputs.api_url }}
      web_url: ${{ steps.outputs.outputs.web_url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment
        uses: ./.github/actions/setup-env

      - name: Install dependencies
        uses: ./.github/actions/install-deps

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Build and deploy retail UI
        env:
          STACK_NAME: ${{ needs.detect-changes.outputs.base_stack_name }}
          VERTICAL: retail
        run: |
          chmod +x scripts/deploy-ui.sh
          ./scripts/deploy-ui.sh

      - name: Get stack outputs
        id: outputs
        run: |
          STACK_NAME="${{ needs.detect-changes.outputs.base_stack_name }}-retail"

          OUTPUTS=$(aws cloudformation describe-stacks \
            --stack-name "${STACK_NAME}" \
            --query 'Stacks[0].Outputs' \
            --output json)

          API_URL=$(echo "$OUTPUTS" | jq -r '.[] | select(.OutputKey=="RetailApiUrl") | .OutputValue')
          WEB_URL=$(echo "$OUTPUTS" | jq -r '.[] | select(.OutputKey=="RetailUiBucketWebsiteURL") | .OutputValue')

          echo "api_url=${API_URL}" >> $GITHUB_OUTPUT
          echo "web_url=${WEB_URL}" >> $GITHUB_OUTPUT

  retail-tests:
    name: Retail E2E Tests
    needs: [deploy-retail-infra, deploy-retail-ui]
    if: always() && needs.deploy-retail-ui.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment for E2E tests
        uses: ./.github/actions/setup-env

      - name: Install Python dependencies for E2E tests
        run: pip install -r requirements-test.txt

      - name: Configure AWS credentials for E2E tests
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Install Chrome and ChromeDriver
        uses: browser-actions/setup-chrome@v1

      - name: Run retail E2E tests
        env:
          RETAIL_URL: ${{ needs.deploy-retail-ui.outputs.web_url }}
          RETAIL_API_URL: ${{ needs.deploy-retail-ui.outputs.api_url }}
          HEADLESS: 'true'
        run: |
          cd tests/e2e
          pytest -v --tb=short -m "retail"

  seed-retail-data:
    name: Seed Retail Data
    needs: [deploy-retail-ui, retail-tests]
    if: |
      always() &&
      needs.deploy-retail-ui.result == 'success' &&
      (needs.retail-tests.result == 'success' || needs.retail-tests.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Seed retail demo data
        uses: ./.github/actions/clear-and-seed-data
        with:
          api-base-url: ${{ needs.deploy-retail-ui.outputs.api_url }}
          vertical: retail

  # ========================================
  # Landing Page Pipeline
  # ========================================

  deploy-landing-ui:
    name: Deploy Landing UI
    needs: [detect-changes]
    if: |
      always() &&
      (needs.detect-changes.outputs.ui_changed == 'true' || inputs.force_deploy == 'true')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      web_url: ${{ steps.deploy.outputs.web_url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: ./.github/actions/setup-env

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Deploy Landing UI
        id: deploy
        env:
          VERTICAL: landing
          STACK_NAME: ${{ needs.detect-changes.outputs.base_stack_name }}-landing
        run: ./scripts/deploy-ui.sh

      - name: Set output
        run: |
          WEB_URL=$(aws cloudformation describe-stacks \
            --stack-name "${{ needs.detect-changes.outputs.base_stack_name }}-landing" \
            --query 'Stacks[0].Outputs[?OutputKey==`LandingUiBucketWebsiteURL`].OutputValue' \
            --output text 2>/dev/null || echo "")
          echo "web_url=${WEB_URL}" >> $GITHUB_OUTPUT

  landing-tests:
    name: Landing E2E Tests
    needs: [detect-changes, deploy-landing-ui]
    if: always() && needs.deploy-landing-ui.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment
        uses: ./.github/actions/setup-env

      - name: Install Python dependencies
        run: pip install -r requirements-test.txt

      - name: Install Chrome and ChromeDriver
        uses: browser-actions/setup-chrome@v1

      - name: Run landing E2E tests
        id: test-run
        continue-on-error: true
        env:
          LANDING_URL: ${{ needs.deploy-landing-ui.outputs.web_url }}
          HEADLESS: 'true'
        run: |
          set -o pipefail
          echo "Running landing E2E tests..."
          cd tests/e2e
          pytest -v --tb=short -m "landing" 2>&1 | tee ../../landing-e2e-tests-output.txt

      - name: Upload test output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: landing-e2e-test-output
          path: landing-e2e-tests-output.txt
          retention-days: 7

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: landing-e2e-test-artifacts
          path: tests/e2e/test-artifacts/
          retention-days: 7
          if-no-files-found: ignore

      - name: Check test outcome
        if: always()
        run: |
          if [ "${{ steps.test-run.outcome }}" = "failure" ]; then
            echo "Landing E2E tests failed"
            exit 1
          fi

  # ========================================
  # Post-Deployment Summary
  # ========================================

  pr-summary:
    name: Post PR Summary
    needs: [
      detect-changes,
      deploy-insurance-infra,
      deploy-insurance-ui,
      insurance-tests,
      seed-insurance-data,
      deploy-retail-infra,
      deploy-retail-ui,
      retail-tests,
      seed-retail-data,
      deploy-landing-ui,
      landing-tests
    ]
    if: always() && github.event.pull_request.number
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build PR summary comment
        id: summary
        run: |
          cat > /tmp/pr-comment.md <<'EOF'
          ## 🚀 Multi-Vertical Test Deployment Summary

          **PR**: #${{ github.event.pull_request.number }}
          **Workflow Run**: [View Details](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})

          EOF

          # Add deployment status table
          cat >> /tmp/pr-comment.md <<EOF
          ### Deployment Status

          | Vertical | Infrastructure | UI | Tests | Data Seeding |
          |----------|---------------|-----|-------|--------------|
          | 🏥 **Insurance** | ${{ needs.deploy-insurance-infra.result == 'success' && '✅' || needs.deploy-insurance-infra.result == 'skipped' && '⏭️' || '❌' }} | ${{ needs.deploy-insurance-ui.result == 'success' && '✅' || needs.deploy-insurance-ui.result == 'skipped' && '⏭️' || '❌' }} | ${{ needs.insurance-tests.result == 'success' && '✅' || needs.insurance-tests.result == 'skipped' && '⏭️' || '❌' }} | ${{ needs.seed-insurance-data.result == 'success' && '✅' || needs.seed-insurance-data.result == 'skipped' && '⏭️' || '❌' }} |
          | 🛒 **Retail** | ${{ needs.deploy-retail-infra.result == 'success' && '✅' || needs.deploy-retail-infra.result == 'skipped' && '⏭️' || '❌' }} | ${{ needs.deploy-retail-ui.result == 'success' && '✅' || needs.deploy-retail-ui.result == 'skipped' && '⏭️' || '❌' }} | ${{ needs.retail-tests.result == 'success' && '✅' || needs.retail-tests.result == 'skipped' && '⏭️' || '❌' }} | ${{ needs.seed-retail-data.result == 'success' && '✅' || needs.seed-retail-data.result == 'skipped' && '⏭️' || '❌' }} |

          EOF

          # Add environment URLs if successful
          INSURANCE_UI_SUCCESS="${{ needs.deploy-insurance-ui.result == 'success' }}"
          RETAIL_UI_SUCCESS="${{ needs.deploy-retail-ui.result == 'success' }}"
          INSURANCE_SEED_SUCCESS="${{ needs.seed-insurance-data.result == 'success' }}"
          RETAIL_SEED_SUCCESS="${{ needs.seed-retail-data.result == 'success' }}"

          if [ "$INSURANCE_UI_SUCCESS" = "true" ] || [ "$RETAIL_UI_SUCCESS" = "true" ]; then
            cat >> /tmp/pr-comment.md <<EOF
          ---

          ### 🌐 Test Environments

          EOF

            if [ "$INSURANCE_UI_SUCCESS" = "true" ]; then
              INSURANCE_STATUS=""
              if [ "$INSURANCE_SEED_SUCCESS" = "true" ]; then
                INSURANCE_STATUS=" ✅ Ready with demo data"
              else
                INSURANCE_STATUS=" ⚠️ Deployed (demo data pending)"
              fi
              cat >> /tmp/pr-comment.md <<EOF
          #### 🏥 Insurance Vertical${INSURANCE_STATUS}
          **Web URL:** ${{ needs.deploy-insurance-ui.outputs.web_url }}
          **API URL:** ${{ needs.deploy-insurance-ui.outputs.api_url }}

          EOF
            fi

            if [ "$RETAIL_UI_SUCCESS" = "true" ]; then
              RETAIL_STATUS=""
              if [ "$RETAIL_SEED_SUCCESS" = "true" ]; then
                RETAIL_STATUS=" ✅ Ready with demo data"
              else
                RETAIL_STATUS=" ⚠️ Deployed (demo data pending)"
              fi
              cat >> /tmp/pr-comment.md <<EOF
          #### 🛒 Retail Vertical${RETAIL_STATUS}
          **Web URL:** ${{ needs.deploy-retail-ui.outputs.web_url }}
          **API URL:** ${{ needs.deploy-retail-ui.outputs.api_url }}

          EOF
            fi
          fi

          # Add failure notice if any failures
          if [ "${{ needs.deploy-insurance-infra.result }}" = "failure" ] || \
             [ "${{ needs.deploy-insurance-ui.result }}" = "failure" ] || \
             [ "${{ needs.insurance-tests.result }}" = "failure" ] || \
             [ "${{ needs.deploy-retail-infra.result }}" = "failure" ] || \
             [ "${{ needs.deploy-retail-ui.result }}" = "failure" ] || \
             [ "${{ needs.retail-tests.result }}" = "failure" ]; then
            cat >> /tmp/pr-comment.md <<EOF
          ---

          ⚠️ **Some deployments or tests failed.** Check the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details.
          EOF
          fi

          echo "comment_file=/tmp/pr-comment.md" >> $GITHUB_OUTPUT

      - name: Post comment to PR
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const commentBody = fs.readFileSync('/tmp/pr-comment.md', 'utf8');

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: commentBody
            });
